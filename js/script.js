
$(".owl-carousel").owlCarousel({
    autoplay: true,
    loop: true,
    items: 1,
    // stagePadding: 10,
});

AOS.init({
    debounceDelay: 50, 
    throttleDelay: 99, 
    offset: 120, 
    delay: 0, 
    duration: 400, 
    easing: 'ease', 
    once: true, 
  });

